import java.awt.BorderLayout;

import javax.swing.*;
public class MancalaBoard extends JPanel {

	/**
	 * creates MancalaBoard Panel with the Mancala board layout
	 */
	public MancalaBoard(){
		setSize(800,400);
		setLayout(new BorderLayout()); 
		add(new PlayerPanel(), BorderLayout.CENTER);
	}
}
